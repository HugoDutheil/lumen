#General settings of the experiment
experiment: 
  name: "Training"
  weights_filename: "model_weights.pth"
training: 
  num_samples: 20000
  train_split: 0.8
  n_epochs: 100
  lr: 0.001
model: 
  hidden_dim: 64